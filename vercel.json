{
  "builds": [
    {
      "src": "src/pages/**/*.html",
      "use": "@vercel/static",
      "scripts": {
        "build": "npx tailwindcss -i ./src/input.css -o ./dist/output.css --minify"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/src/pages/$1" }]
}
